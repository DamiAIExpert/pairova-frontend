import Address from "@/components/jobSeeker/onboarding/address";
import { ScrollRestoration } from "react-router";

const AddressPage = () => {
  return (
    <div>
      <ScrollRestoration />
      <Address />
    </div>
  );
};

export default AddressPage;
