import Bio from "@/components/jobSeeker/onboarding/bio";
import { ScrollRestoration } from "react-router";

const BioPage = () => {
  return (
    <div>
      <ScrollRestoration />
      <Bio />
    </div>
  );
};

export default BioPage;
